# Keep exactly ONE OpenCV build installed. Multiple cv2 wheels (e.g. -python +
# -python-headless + -contrib) leave duplicate native symbols that segfault on
# ARM64. RapidOCR pulls opencv-python; do not add another opencv-* package.
opencv-python>=4.9,<4.11
numpy>=1.26,<2
Pillow>=10.2
# OCR runs on onnxruntime, not paddlepaddle. Paddle's PyPI aarch64 wheels
# segfault during inference on the Raspberry Pi 5; RapidOCR uses the same PP-OCR
# models exported to ONNX, which run stably on ARM64.
rapidocr-onnxruntime>=1.3
onnxruntime>=1.17
ollama>=0.1.7
python-docx>=1.1
fpdf2>=2.7
Flask>=3.0
PyJWT>=2.8
bcrypt>=4.1
requests>=2.31
nltk>=3.8
jiwer>=3.0
rouge-score>=0.1.2
